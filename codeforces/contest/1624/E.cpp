#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define mod 1000000007

void solve()
{
    char ch;
    cin >> ch;

    int n, m;
    cin >> n >> m;

    string s[n];
    for (int i = 0; i < n;i++)
        cin >> s[i];
    string phone;
    cin >> phone;

    int pos = 0;
    
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    ll test=1;
    cin>>test;
    while(test--)
    {
        solve();
    }
    return 0;
}
